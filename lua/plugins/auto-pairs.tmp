return {{
  'windwp/nvim-autopairs',
  event = "InsertEnter",
  config = function()
    local npairs = require('nvim-autopairs')
    npairs.setup({
      map_bs = true,  -- Enable backspace to delete pairs
      map_cr = true,  -- Enable Enter to complete pairs
    })
  end
},}
